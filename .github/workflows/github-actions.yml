name: Deps, lint and test
on: [push]
jobs:
  Deps-Lint-Test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm i -g yarn
      - run: yarn
      - run: yarn lint
      - run: yarn test:unit
